# ETF 量化轮动系统项目规则

## 项目定位

- 本项目是 ETF 量化轮动系统，包含根目录 JavaScript 实时雷达、
  `etf-rotation-v6` Python 研究回测和 `ETF-Quant-System` 历史研究框架。
- `etf-rotation-v6` 是策略定义、参数、回测和研究结论的唯一真源。
- 根目录 JavaScript 仅是独立实时雷达，不得用 V6 回测为其建议背书；
  `ETF-Quant-System` 仅为历史兼容研究框架，不得作为当前 V6 绩效来源。
- 风险控制优先于提高历史收益。不得以提高样本内收益为理由削弱风控、扩大前视边界
  或忽略交易成本。
- 本项目仅用于研究和教育。不得自动连接券商或执行实盘下单。

## 数据与时序

- 禁止未来函数、数据泄漏和任何形式的样本外信息回写。
- 第 T 日收盘后产生的信号，最早只能在 T+1 交易；成交价格必须与数据可用时间一致。
- 若采用 T+1 开盘、收盘、VWAP 或其他价格，必须在配置、代码和测试中使用同一语义。
- 回测必须计入手续费和滑点。成本费率、计费方向和扣费时点必须可配置并有测试。
- 新上市、停牌、缺失行情、复权、退市和历史 ETF 池规则未明确时，统一标记为
  “待确认”，不得静默假设。

## 参数和策略变更

- 参数必须存放在配置文件中，不得写死在策略、组合或回测代码里。
- 任何缺失或尚未达成一致的规则必须明确标记为“待确认”。
- 修改策略前必须先运行基准测试并保存对照结果。
- 修改策略后必须运行全部测试和同口径基准回测。
- 必须比较年化收益、最大回撤、夏普、卡玛、换手率和样本外表现。
- 不得只根据总收益判断策略是否改善。
- 不得为了改善单次历史回测而反复搜索参数；参数选择期与样本外验证期必须分离。
- 未经明确授权，不得修改当前买卖规则、策略参数或基准结果。

## 测试和完成标准

- 根目录 JavaScript 测试：`npm test`
- V6 Python 测试：在 `etf-rotation-v6` 中运行
  `python -m unittest discover -s tests -v`
- 旧框架 Python 测试：在 `ETF-Quant-System` 中运行
  `python -m unittest discover -s test -v`
- 策略相关变更必须覆盖信号时序、手续费、滑点、仓位上限、总暴露、回撤风控、
  暖机期和缺失数据。
- 测试失败时不得声称任务完成。必须报告失败测试、错误信息和未完成事项。
- 基准结果只有在数据、参数、成交模型和代码版本均明确时才可比较。

## 安全与隐私

- 不得提交 `.env`、Token、API 密钥、SSH 密钥、券商账号、交易密码或私人交易数据。
- 不得在日志、文档、测试输出、提交信息或对话中显示任何密钥和敏感信息。
- 运行时个人组合数据不得默认提交；其归档和脱敏规则为“待确认”。
- 不得自动连接券商、创建交易凭据或执行模拟盘/实盘下单。

## 仓库协作

- 修改前先检查 `git status`，保留用户已有和未跟踪的工作。
- 不得把 `etf-rotation-v6`、`ETF-Quant-System` 和根目录 JavaScript 代码视为可互换实现，
  也不得把非 V6 输出标记为 V6 结果。
- 策略定义、参数含义或执行口径冲突时，先更新 `docs/unknowns.md` 并请求确认。
- 审计基线见 `docs/repo_audit.md`；开发门禁见 `docs/development_workflow.md`。
