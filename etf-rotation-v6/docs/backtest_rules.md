# 回测规则

1. 每个交易日的因子只读取该日及以前的价格。
2. 交易日 `t` 形成的目标权重，在 `t+1` 才参与收益计算。
3. 仅在配置的调仓间隔更新目标，调仓之间持仓延续。
4. 单标的权重不得超过 `max_position`。
5. 市场状态控制组合总风险暴露，未使用权重为现金。
6. 换手率乘以单边成本，从当日组合收益中扣除。
7. 净值从高点回撤达到阈值后清仓并进入固定冷却期。
8. 缺失收益按零处理仅用于样例；正式数据应先完成上市期和停牌规则。

必须通过 `test_no_lookahead.py`，任何使用未来价格修正历史信号的实现均视为失败。
