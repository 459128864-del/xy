import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import {confirmedPivots,divergence,validateBars,tradeGate,suggestedQty,Replay,DailyState} from '../core.js';
const cfg=JSON.parse(fs.readFileSync(new URL('../config.example.json',import.meta.url)));const bar=(i,low=10,high=11,volume=100,difbps=0)=>({time:`2026-08-12T${String(9+Math.floor(i/12)).padStart(2,'0')}:${String(i*5%60).padStart(2,'0')}:00`,open:10,high,low,close:10.5,volume,difbps,index:i});
test('摆动点等待右侧两根，禁止未来函数',()=>{let xs=[bar(0,3),bar(1,2),bar(2,1),bar(3,2),bar(4,3)];assert.equal(confirmedPivots(xs,cfg,3).lows.length,0);assert.equal(confirmedPivots(xs,cfg,4).lows[0].confirmedAt,4)});
test('标准底背离、近似双底和标准顶背离',()=>{let a={index:1,low:10,high:10,volume:100,difbps:-5},b={index:5,low:9.9,high:11,volume:90,difbps:-2};assert.equal(divergence([a,b],'bottom',cfg).standard,true);b.low=10.01;assert.equal(divergence([a,b],'bottom',cfg).standard,false);a.high=10;b.high=10.1;a.difbps=5;b.difbps=2;assert.equal(divergence([a,b],'top',cfg).standard,true)});
test('放量破位、重复乱序与零量安全失败',()=>{assert.equal(validateBars([bar(0),bar(1,10,11,0)]).ok,false);assert.equal(validateBars([bar(1),bar(0)]).ok,false);let d=divergence([{index:1,low:10,volume:100,difbps:-5},{index:5,low:9.9,volume:130,difbps:-2}],'bottom',cfg);assert.equal(d.volumeContracted,false)});
test('RR、毛价差、成本后净价差硬门禁',()=>{assert.equal(tradeGate({direction:'forward',entry:10,stop:9.8,target:10.5,atr20:.2,prevClose:10,score:.8,cfg}).pass,true);assert.ok(tradeGate({direction:'forward',entry:10,stop:9,target:10.01,atr20:.2,prevClose:10,score:.8,cfg}).failures.length)});
test('100股取整及现金旧仓风险三重约束',()=>{assert.equal(suggestedQty({availableOldQty:5000,cashAvailable:100000,accountEquity:1000000,entry:10,stop:9,cfg}).qty,700);assert.equal(suggestedQty({availableOldQty:5000,cashAvailable:NaN,accountEquity:1e6,entry:10,stop:9,cfg}).qty,null)});
test('一天一轮、方向锁定及时间门禁',()=>{let s=new DailyState;assert.equal(s.open('A','2026-01-01','forward','09:39',cfg),false);assert.equal(s.open('A','2026-01-01','forward','10:00',cfg),true);assert.equal(s.open('A','2026-01-01','reverse','10:30',cfg),false);s.restore('A','2026-01-01');assert.equal(DailyState.from(s.serialize()).days['2026-01-01:A'].state,'DONE')});
test('14:30后不开循环，回放只能逐bar',()=>{let s=new DailyState;assert.equal(s.open('A','d','forward','14:31',cfg),false);let r=new Replay([bar(0),bar(1)]);assert.equal(r.next().length,1);assert.equal(r.future,undefined);assert.equal(r.next().length,2)});
test('午间休市缺口不会静默填充',()=>{let xs=[bar(0),{...bar(1),time:'2026-08-12T13:05:00'}];assert.equal(validateBars(xs).ok,true);assert.equal(xs.length,2)});
